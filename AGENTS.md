# 仓库工作规则

## 安全

- 将本仓库之外的原始收藏视为只读内容。
- 除非用户明确变更任务范围，否则不得修改 `data/`、重新生成或编辑 `reports/` 下的历史产物，也不得修改 `SillyInnkeeper-main/`。
- 执行纯文档工作时，不得运行分类、收集、整理或外部模型命令。
- 保留用户已有的修改。编辑前检查 `git status` 和相关差异。

## 文档职责

- `README.md` 是精简的项目入口，只包含项目用途、安全边界、当前状态摘要、快速开始和文档导航。
- `docs/status.md` 是当前状态的唯一权威来源。只能依据可验证的代码和产物更新它。
- `docs/history/` 保存带日期的历史快照。必须保留其中的日期、数量、哈希、批次路径和当时结论；不得为了符合当前状态而静默改写。
- `CONTEXT.md` 只能作为术语表。领域术语变化时应同步更新，但不得加入实现细节、命令、路径、阶段状态或决策记录。
- `docs/runbook.md` 负责记录操作命令及其安全警告。
- `docs/architecture.md` 负责记录系统结构、脚本职责、数据流和输入输出关系。
- `分类标准.md` 是分类规则的唯一权威来源。`docs/classification-policy.md` 可以说明其使用方式，但不得复制规则正文或类别列表。
- 只有当一个决策难以逆转、缺少上下文时会令人困惑，并且确实经过替代方案取舍时，才允许创建 ADR。

## 验证

- 文档与代码不一致时，行为以当前代码为准，已完成工作以不可变批次元数据或执行日志为准。应记录差异，不得将其抹去。
- 对照当前源码核实文档中的每个脚本、位置参数、选项、默认输入、输出目录和副作用。
- 当前文档使用仓库相对路径。仅在明确标注的历史快照或具体本地示例中使用单台机器专属的绝对路径。
- 完成文档修改前，检查 Markdown 链接、标题层级、围栏代码块语言以及职责重复问题。
- 如果政策修改改变了 `分类标准.md`，必须记住：已完成的模型批次绑定其 SHA-256，文件哈希变化后不能继续续跑原批次。
