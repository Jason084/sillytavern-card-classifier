# 领域术语

[English](./CONTEXT.md)

**角色卡**：可供对话系统使用的逻辑角色定义；同一角色卡可以存在于多个物理文件中。

**文件记录**：一次扫描中代表一个物理文件的审计条目，不能与角色卡混用。

**唯一内容**：排除不改变角色定义的文件级差异后，可作为同一逻辑对象处理的角色卡内容。

**运行批次**：一次阶段执行产生的、具有独立身份且不可覆盖更新的快照。

**分类**：依据政策选择一个核心类别的决定；它与排除、人工复核互斥。

**排除**：确定命中绝对政策规则的决定，不表示删除文件。

**人工复核**：需要人作出最终判断的待决状态，不是普通类别。

**细分**：在不改变一级决定的前提下，为符合条件的同人角色卡增加作品或 IP 分组。

**整理计划**：包含来源、目标、操作和完整性信息的一组预期文件操作；它尚未改变角色卡文件。

**批准执行**：应用一份明确经人工批准的整理计划，并记录每条结果。

**权威输入**：后续阶段明确选用并验证过的输入快照，不等于任意“最新目录”。
