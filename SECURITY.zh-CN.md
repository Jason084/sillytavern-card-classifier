# 安全政策

[English](./SECURITY.md)

## 支持版本

在引入正式版本发布前，只为 `main` 的最新提交提供安全修复。

## 报告方式

私有预览期间，请通过已有的私密审核渠道联系仓库所有者。仓库转为公开时会同步启用 GitHub 私密漏洞报告，公开后请使用该功能。报告应包含受影响阶段、基于合成数据的复现步骤、影响和建议缓解方式。不要附加真实角色卡、API 密钥、私人报告或个人路径。

## 凭据与数据处理

- 模型凭据只保存在进程环境变量，或你自行选择且被忽略的本地 `.env` 流程中。
- 任何曾写入跟踪文件、终端记录、报告或 Issue 的凭据都应立即轮换。
- 把模型端点视为外部数据处理方；使用真实收藏前先检查 `cardModelInput` 发送的字段。
- 除非独立审核过内容与权利，不要公开 `data/` 和 `reports/`。

维护者会尽快确认有效报告，并在修复可用后协调披露。
