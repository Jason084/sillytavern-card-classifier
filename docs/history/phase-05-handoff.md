# 第五阶段模型分类结果与交接

> **历史快照（2026-08-22）**：本文保留第五阶段完成时的路径、数字、哈希、风险与当时结论，不代表当前状态。请以[当前状态](../status.md)为准。

> **后续状态注记（2026-08-28）**：下文“第六阶段尚未启动”只描述本快照形成时的状态。第六阶段计划 `20260822T131941624Z-0ab16690` 后来已获批准并执行；同批次的 `execution-summary-20260822T132041875Z-58d8b962.json` 记录 15,134 条计划全部为 `copied`。本注记只补充后续事实，不改写当时的交接结论。

更新日期：2026-08-22
项目目录：`C:\ancode\Claude Code\test\角色卡分类`
交接范围：第五阶段全量分类（05）、第一次模型复核（051）、第二次模型复核（052）

## 一、当前结论

第五阶段的三轮模型处理已经全部完成，检查点、结果映射和错误文件均已验收。后续应以 052 批次的完整合并结果为唯一最新结果：

`reports/classification-reviews-052/20260822T121236978Z-f3d98577/classifications.jsonl`

最终 15,134 条有效文件记录的结果为：

| 最终决定 | 文件记录 | 唯一角色卡内容 | 说明 |
|---|---:|---:|---|
| `classify` | 12,754 | 12,545 | 已获得一个主分类 |
| `exclude` | 2,075 | 2,022 | 模型明确判定命中绝对避雷，仍保留复核标记，不能进入整理 |
| `review` | 305 | 300 | 三轮后仍需要人工判断 |
| 合计 | 15,134 | 14,867 | 与扫描索引完全一致 |

`needs_review=true` 的文件记录总数是 2,380，而不是 305。其构成为 2,075 条 `exclude` 加 305 条真正未决的 `review`。这是有意保留的保守语义：明确排除项同样不能被第六阶段当作普通分类整理。

第六阶段尚未启动。开始第六阶段前至少还要处理：

1. 305 条最终 `review`。
2. 05 首轮产生并保留至今的 6 个标准外分类名，共 171 条文件记录、169 个唯一内容。
3. 已知的 5 个英文卡明显漏判候选。
4. 已知的 3 个 R18G 边界复核候选。
5. 明确 2,075 条 `exclude` 的最终处置方式；当前代码不会删除或移动它们。

## 二、数据与规则基线

本轮处理使用以下固定输入：

| 项目 | 值 |
|---|---|
| 扫描批次 | `reports/scans/20260821T062149Z/index.jsonl` |
| 扫描索引 SHA-256 | `d8e30947987603546f7dd2f84e8a06293e7fca6a9abbe0ac28fe32746e56fa6f` |
| 分类标准 | `分类标准.md` |
| 分类标准 SHA-256 | `185f7c7fe17506b2975af2b31332385b57c4b47b502ccc2640271f0d80b04fa7` |
| 扫描记录 | 15,795 |
| 有效角色卡文件 | 15,134 |
| 唯一角色卡内容 | 14,867 |
| 模型接口 | `https://aibh.cc/v1` |
| 实际模型 ID | `v4 flash` |

接口别名 `flash` 会返回 `invalid model`，不要改回该别名。

模型输入继续使用 `src/lib/model-client.mjs` 中的裁剪字段，没有扩大 R18G 检查范围。这是此前已经确认的边界：如果危险内容藏在当前裁剪范围之外，模型和常规人工预览都可能无法发现，本阶段不为此重新扫描或无限扩大正文。

## 三、三轮执行记录

### 3.1 05：全量分类

| 项目 | 值 |
|---|---|
| 脚本 | `src/050-classify-character-cards.mjs` |
| 批次 | `reports/classifications/20260822T090050564Z-ecf9b34a` |
| 提示词版本 | `classification-v6-aibh-unrestricted-prefix` |
| 批量 | 30 |
| 实际并发 | 5 |
| 最多重试 | 2 |
| 请求上限 | 实际取消；元数据使用极大整数兼容旧预算器 |
| HTTP 请求预留 | 638 |
| 完成状态 | `complete` |

05 对 14,867 个唯一内容完成全量判断，并映射回 15,134 个文件：

| 05 决定 | 文件记录 |
|---|---:|
| `classify` | 11,089 |
| `exclude` | 2,571 |
| `review` | 1,474 |
| 首轮 `needs_review` 合计 | 4,045 |

05 结果文件 SHA-256：

`2644f0240c4480bfe72c39f49605261207414371279c32cfc9fdc80cdec97fc0`

### 3.2 051：复核全部 05 人工复核项

| 项目 | 值 |
|---|---|
| 脚本 | `src/051-classify-character-cards.mjs` |
| 批次 | `reports/classification-reviews/20260822T112558723Z-741b9c83` |
| 提示词版本 | `classification-review-v1-aibh-batch10` |
| 目标 | 05 中全部 `needs_review=true` |
| 目标文件/唯一内容 | 4,045 / 3,949 |
| 批量/并发/最多重试 | 10 / 5 / 2 |
| HTTP 请求预留 | 419 |
| 完成状态 | `complete` |

051 的文件级转移结果：

| 转移 | 文件记录 |
|---|---:|
| `exclude -> classify` | 839 |
| `exclude -> exclude` | 1,685 |
| `exclude -> review` | 47 |
| `review -> classify` | 698 |
| `review -> exclude` | 313 |
| `review -> review` | 463 |

因此 051 共释放 1,537 条到普通分类，确认 1,998 条排除，留下 510 条 `review`。

051 结果文件 SHA-256：

`95c52158c58292c61f57faabfd0ea40179e2084330bbffbf076e83296bd5e4a2`

### 3.3 052：再次复核 051 剩余 review

| 项目 | 值 |
|---|---|
| 脚本 | `src/052-classify-character-cards.mjs` |
| 批次 | `reports/classification-reviews-052/20260822T121236978Z-f3d98577` |
| 提示词版本 | `classification-review-v2-aibh-batch2` |
| 目标 | 051 已完成且仍为 `review` 的记录 |
| 目标文件/唯一内容 | 510 / 501 |
| 批量/并发/最多重试 | 2 / 2 / 3 |
| HTTP 请求预留 | 258 |
| 完成状态 | `complete` |

052 的文件级转移结果：

| 转移 | 文件记录 |
|---|---:|
| `review -> classify` | 128 |
| `review -> exclude` | 77 |
| `review -> review` | 305 |

052 没有请求 051 已分类或已排除的内容。510 条目标全部完成，501 个检查点哈希均唯一且全部属于目标集合。

## 四、最终分类分布

以下为 052 完整合并结果中 `classify` 的分布。文件数用于实际整理，唯一内容数用于理解重复卡影响。

| 分类 | 文件数 | 唯一内容数 | 标准状态 |
|---|---:|---:|---|
| 同人 | 2,250 | 2,208 | 标准内 |
| 现代都市 | 1,696 | 1,665 | 标准内 |
| 伦理禁忌 | 1,272 | 1,241 | 标准内 |
| 校园 | 1,210 | 1,188 | 标准内 |
| 西方奇幻 | 927 | 915 | 标准内 |
| 科幻末世 | 562 | 558 | 标准内 |
| 调教 | 531 | 516 | 标准内 |
| NTR | 528 | 522 | 标准内 |
| 古风历史 | 472 | 470 | 标准内 |
| 修仙玄幻 | 454 | 448 | 标准内 |
| 纯爱 | 375 | 368 | 标准内 |
| 规则模拟 | 359 | 356 | 标准内 |
| 网络爽文与系统 | 338 | 334 | 标准内 |
| 后宫 | 303 | 298 | 标准内 |
| 悬疑惊悚 | 238 | 235 | 标准内 |
| 风俗体验 | 231 | 228 | 标准内 |
| 工具整活 | 188 | 187 | 标准内 |
| 整活 | 184 | 182 | 标准内 |
| 催眠 | 152 | 149 | 标准内 |
| 病娇 | 121 | 120 | 标准内 |
| 经营模拟 | 111 | 109 | **标准外** |
| 网红 | 111 | 110 | 标准内 |
| 直播 | 81 | 78 | 标准内 |
| 人妻公寓 | 40 | 40 | **标准外** |
| 系统 | 12 | 12 | **标准外** |
| 聊天群 | 4 | 4 | **标准外** |
| 贴吧 | 2 | 2 | **标准外** |
| 追夫 | 2 | 2 | **标准外** |

051 和 052 已将新产生的分类严格限制在当前分类标准中，但它们不会改写 05 中原本已经 `classify` 的记录。因此，最终结果仍保留 05 首轮创建的 6 个标准外类别，共 171 条文件、169 个唯一内容。进入第六阶段前必须由用户决定这些类别是保留还是映射到标准类别；不要让接手者自行猜测。

## 五、剩余 305 条 review

最终 `review` 位于 052 的完整结果中：

`reports/classification-reviews-052/20260822T121236978Z-f3d98577/classifications.jsonl`

筛选条件：

```text
model_decision == "review"
```

052 的 `review.csv` 包含本轮全部 510 条目标；若从该表工作，应筛选：

```text
third_decision == "review"
```

根据 052 简短理由进行的交接性归纳如下。该归纳只是便于分工的关键词汇总，最终仍以逐条内容和分类标准为准：

| 大致原因 | 文件数 | 说明 |
|---|---:|---|
| 核心男娘、扶她、伪娘、性转等性别属性 | 294 | 当前标准明确要求核心角色或核心玩法时人工判断 |
| 内容不足、裁剪不足或正文加密 | 7 | 现有模型输入无法可靠下结论 |
| 年龄/视觉年龄边界 | 2 | 052 主动保留人工判断 |
| 中英文比例边界 | 1 | 需确认正常游玩是否长期阅读英文 |
| R18G 边界 | 1 | 需确认是否达到明确血腥猎奇标准 |
| 合计 | 305 | 对应 300 个唯一内容 |

不建议继续无条件复制为 053 并使用同一模型重复投票。剩余项目大多不是模型信心不足，而是分类标准本身明确指定为人工判断；继续用相同模型和相同裁剪输入，收益预计会明显递减。

## 六、已知漏判和待确认风险

### 6.1 英文卡明显漏判候选

此前按“实际裁剪输入没有汉字且至少有 500 个拉丁字母”的保守启发式审计，发现以下 5 条仍被归入普通分类；051、052 因它们在 05 已是 `classify`，没有重新请求：

1. `Bronya (APHO).card.png` → 同人
2. `Evelynn.png` → 病娇
3. `Minami的50 50赌博.png` → NTR
4. `一个在悲剧中死去并重生为僵尸的充满爱心的女人，她现在是你的女仆，但你能安抚她受折磨的灵魂吗？.png` → 西方奇幻
5. `一个爱你并想要拥抱的健康女朋友.png` → 纯爱

这 5 条是明确的定向复核候选，不代表启发式覆盖所有英文卡。

### 6.2 R18G 候选

精确标签和关键词审计中，大量命中其实是“禁止 R18G”“不接受 R18G”、普通战斗、恐怖氛围或内容等级说明，不能机械排除。以下 3 条的现有裁剪文本更值得单独人工确认，而且当前仍为普通分类：

1. `婊子打手召唤系统1_1 (3).png` → 网络爽文与系统；文本曾出现允许血腥、猎奇或 R18G 的表述。
2. `刘雨瑶，一名外表清纯、内心极度放荡且精于算计的女大学生.png` → 校园；文本曾出现不回避 R18/R18G 的表述。
3. `91聊天群.png` → 调教；文本曾出现“重口血腥”玩法表述。

此前更宽泛的关键词扫描命中 547 个文件、535 个唯一内容，但大多数是上下文误报，因此不能把该数字当作漏判数，也不应批量改为排除。

## 七、产物、哈希与用途

### 7.1 05 批次

目录：`reports/classifications/20260822T090050564Z-ecf9b34a`

- `checkpoint.jsonl`：14,867 个唯一内容的首轮结果。
- `classifications.jsonl`：15,134 条首轮文件结果。
- `review.csv`：首轮全量复核表。
- `usage.jsonl`：请求预留和完成事件，不含密钥和正文提示词。
- `summary.json`、`run.json`：统计和批次绑定信息。

### 7.2 051 批次

目录：`reports/classification-reviews/20260822T112558723Z-741b9c83`

- `checkpoint.jsonl`：3,949 个唯一目标的二次决定。
- `rechecked-classifications.jsonl`：4,045 条二次复核映射。
- `classifications.jsonl`：合并后的 15,134 条完整结果。
- `review.csv`：首次与二次决定对照。

### 7.3 052 批次（当前权威结果）

目录：`reports/classification-reviews-052/20260822T121236978Z-f3d98577`

- `checkpoint.jsonl`：501 个唯一目标的第三次决定和简短理由。
- `rechecked-classifications.jsonl`：510 条第三次判断映射。
- `classifications.jsonl`：**供后续处理使用的最终完整合并结果**。
- `review.csv`：051 与 052 决定对照。
- `model-errors.jsonl`：空。
- `index-errors.jsonl`：空。
- `summary.json`、`run.json`：最终统计和批次绑定信息。

关键 SHA-256：

| 文件 | SHA-256 |
|---|---|
| 052 `classifications.jsonl` | `46a4ace4ed586a25952760ef0e563d61db2431fbb1fbeb397592d57ee3d936bd` |
| 052 `checkpoint.jsonl` | `e62dd8daacf00771b044bfbf2ed2a4f5a89fb2cd64990d53237dc17146257271` |
| 052 `review.csv` | `a1be0ff0c0e1c0f3b17a403c8692957b992ba7ac2d54c5fba09b5d150ef2730e` |

`reports/` 中包含角色卡衍生数据且被 Git 忽略。交接时必须保留本地报告目录，不能只依赖 Git 仓库恢复这些结果。

## 八、完整性与测试验收

最终验收结果：

- 052 检查点：501 行、501 个唯一哈希，无重复。
- 052 目标：510 个文件、501 个唯一内容；检查点没有目标外哈希。
- 052 复核映射：510 行，全部 `recheck_2_status=complete`。
- 最终合并结果：15,134 行、15,134 个唯一相对路径。
- 最终唯一内容：14,867，与扫描索引一致。
- 052 未触碰的 051 源记录：逐条 JSON 完全一致，差异数 0。
- 051、052 新增的标准外分类：0。
- 052 空判断理由：0。
- `model-errors.jsonl`：0 行。
- `index-errors.jsonl`：0 行。
- `run.json.status`：`complete`。
- 自动化测试：10/10 通过。
- 当前 PowerShell 环境：`MODEL_API_KEY` 未设置。

API 请求日志中的预留次数为：05 共 638、051 共 419、052 共 258。预留事件在发送前写入，因此被人为中断的进程可能留下没有对应完成事件的预留记录：05 有 6 个、051 有 1 个、052 有 4 个。它们不表示分类缺失；最终检查点和结果映射已按内容哈希独立验证完整。

## 九、代码状态

本轮新增或更新：

- `src/051-classify-character-cards.mjs`
- `src/052-classify-character-cards.mjs`
- `test/pipeline.test.mjs`
- `readme.md`
- 本交接文档 `05阶段结果交接.md`

051 和 052 都沿用 05 的独立批次、请求日志、成功即落盘、断点续跑、输入/标准 SHA-256 校验和完整合并机制。额外约束如下：

- 051 只处理 05 的 `needs_review=true`。
- 052 只处理 051 中 `needs_review=true && model_decision=="review" && recheck_status=="complete"`。
- 051、052 的新 `classify` 结果只能使用当前分类标准列出的类别，模型输出标准外类别会被视为无效并重试。
- 052 保留首次、051 和 052 的决定与理由，不覆盖历史批次。

051、052 和对应测试已经进入 Git 历史：`68d8b82`（051）、`55b08d5`（052）、`fc7d03a`（测试），复核阶段使用说明位于 `04abe8c`。本文档写入时，工作树中尚未提交的是本交接文档以及对 `readme.md`“近期状况”的更新。接手者提交前仍应先查看 `git status` 和 `git diff`，不要误删用户已有内容。报告目录被 Git 忽略，不会显示在状态中。

## 十、第六阶段接手顺序

建议接手者按以下顺序继续：

1. 不要重新运行 05、051 或 052，除非分类标准或扫描索引发生变化。三个批次现在都已完整结束。
2. 从最终 `classifications.jsonl` 中筛选 `model_decision=review`，处理 305 条真正未决项目。
3. 决定 6 个标准外类别的保留或映射规则，并以可审计的修正层记录，不要直接手改历史检查点。
4. 定向处理 5 个英文漏判候选和 3 个 R18G 候选。
5. 确认 2,075 条 `exclude` 是否只跳过整理，或另行复制到人工确认的排除目录。当前没有授权删除卡片。
6. 重新校验最终类别集合只包含用户批准的目录名。
7. 明确完成上述事项后，再以 052 批次为输入生成第六阶段预览：

```powershell
node .\src\060-organize-character-cards.mjs `
  .\reports\classification-reviews-052\20260822T121236978Z-f3d98577
```

必须显式传入 052 批次。第六阶段默认查找的是 `reports/classifications`，不会自动选择 `reports/classification-reviews-052`。

第六阶段先生成预览计划，不会立即复制或移动。只有用户检查计划并批准同一批次的 `approval.json` 后才可执行；不得直接修改原始网盘目录。

## 十一、安全交接

- API Sub Key 没有写入代码、配置、报告、日志或本文档。
- Key 曾由用户直接发送在聊天记录中，因此即使本地环境已经清除，也建议任务彻底结束后在服务端撤销并重新生成。
- 原始网盘目录保持只读；本轮只读取项目工作副本和报告。
- 05、051、052 都没有执行文件整理、移动或删除。

## 十二、接手检查清单

- [ ] 本地仍存在三个完整报告批次。
- [ ] 052 `run.json.status` 为 `complete`。
- [ ] 052 `classifications.jsonl` SHA-256 与本文一致。
- [ ] 305 条最终 `review` 已有处理结论。
- [ ] 6 个标准外分类已由用户决定。
- [ ] 5 个英文候选和 3 个 R18G 候选已定向复核。
- [ ] 2,075 条明确排除项的处置方式已得到用户确认。
- [ ] 第六阶段使用显式的 052 批次路径生成预览。
- [ ] 未经批准不执行复制、移动或删除。
